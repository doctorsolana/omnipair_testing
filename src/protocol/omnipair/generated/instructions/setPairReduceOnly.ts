/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { OMNIPAIR_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_PAIR_REDUCE_ONLY_DISCRIMINATOR = new Uint8Array([
  147, 113, 16, 50, 64, 88, 175, 18,
]);

export function getSetPairReduceOnlyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PAIR_REDUCE_ONLY_DISCRIMINATOR,
  );
}

export type SetPairReduceOnlyInstruction<
  TProgram extends string = typeof OMNIPAIR_PROGRAM_ADDRESS,
  TAccountAuthoritySigner extends string | AccountMeta<string> = string,
  TAccountFutarchyAuthority extends string | AccountMeta<string> = string,
  TAccountPair extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthoritySigner extends string
        ? WritableSignerAccount<TAccountAuthoritySigner> &
            AccountSignerMeta<TAccountAuthoritySigner>
        : TAccountAuthoritySigner,
      TAccountFutarchyAuthority extends string
        ? ReadonlyAccount<TAccountFutarchyAuthority>
        : TAccountFutarchyAuthority,
      TAccountPair extends string
        ? WritableAccount<TAccountPair>
        : TAccountPair,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetPairReduceOnlyInstructionData = {
  discriminator: ReadonlyUint8Array;
  reduceOnly: boolean;
};

export type SetPairReduceOnlyInstructionDataArgs = { reduceOnly: boolean };

export function getSetPairReduceOnlyInstructionDataEncoder(): FixedSizeEncoder<SetPairReduceOnlyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["reduceOnly", getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_PAIR_REDUCE_ONLY_DISCRIMINATOR,
    }),
  );
}

export function getSetPairReduceOnlyInstructionDataDecoder(): FixedSizeDecoder<SetPairReduceOnlyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["reduceOnly", getBooleanDecoder()],
  ]);
}

export function getSetPairReduceOnlyInstructionDataCodec(): FixedSizeCodec<
  SetPairReduceOnlyInstructionDataArgs,
  SetPairReduceOnlyInstructionData
> {
  return combineCodec(
    getSetPairReduceOnlyInstructionDataEncoder(),
    getSetPairReduceOnlyInstructionDataDecoder(),
  );
}

export type SetPairReduceOnlyAsyncInput<
  TAccountAuthoritySigner extends string = string,
  TAccountFutarchyAuthority extends string = string,
  TAccountPair extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authoritySigner: TransactionSigner<TAccountAuthoritySigner>;
  futarchyAuthority?: Address<TAccountFutarchyAuthority>;
  pair: Address<TAccountPair>;
  systemProgram?: Address<TAccountSystemProgram>;
  reduceOnly: SetPairReduceOnlyInstructionDataArgs["reduceOnly"];
};

export async function getSetPairReduceOnlyInstructionAsync<
  TAccountAuthoritySigner extends string,
  TAccountFutarchyAuthority extends string,
  TAccountPair extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OMNIPAIR_PROGRAM_ADDRESS,
>(
  input: SetPairReduceOnlyAsyncInput<
    TAccountAuthoritySigner,
    TAccountFutarchyAuthority,
    TAccountPair,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPairReduceOnlyInstruction<
    TProgramAddress,
    TAccountAuthoritySigner,
    TAccountFutarchyAuthority,
    TAccountPair,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? OMNIPAIR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authoritySigner: { value: input.authoritySigner ?? null, isWritable: true },
    futarchyAuthority: {
      value: input.futarchyAuthority ?? null,
      isWritable: false,
    },
    pair: { value: input.pair ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.futarchyAuthority.value) {
    accounts.futarchyAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            102, 117, 116, 97, 114, 99, 104, 121, 95, 97, 117, 116, 104, 111,
            114, 105, 116, 121,
          ]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authoritySigner),
      getAccountMeta(accounts.futarchyAuthority),
      getAccountMeta(accounts.pair),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetPairReduceOnlyInstructionDataEncoder().encode(
      args as SetPairReduceOnlyInstructionDataArgs,
    ),
    programAddress,
  } as SetPairReduceOnlyInstruction<
    TProgramAddress,
    TAccountAuthoritySigner,
    TAccountFutarchyAuthority,
    TAccountPair,
    TAccountSystemProgram
  >);
}

export type SetPairReduceOnlyInput<
  TAccountAuthoritySigner extends string = string,
  TAccountFutarchyAuthority extends string = string,
  TAccountPair extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authoritySigner: TransactionSigner<TAccountAuthoritySigner>;
  futarchyAuthority: Address<TAccountFutarchyAuthority>;
  pair: Address<TAccountPair>;
  systemProgram?: Address<TAccountSystemProgram>;
  reduceOnly: SetPairReduceOnlyInstructionDataArgs["reduceOnly"];
};

export function getSetPairReduceOnlyInstruction<
  TAccountAuthoritySigner extends string,
  TAccountFutarchyAuthority extends string,
  TAccountPair extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OMNIPAIR_PROGRAM_ADDRESS,
>(
  input: SetPairReduceOnlyInput<
    TAccountAuthoritySigner,
    TAccountFutarchyAuthority,
    TAccountPair,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetPairReduceOnlyInstruction<
  TProgramAddress,
  TAccountAuthoritySigner,
  TAccountFutarchyAuthority,
  TAccountPair,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? OMNIPAIR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authoritySigner: { value: input.authoritySigner ?? null, isWritable: true },
    futarchyAuthority: {
      value: input.futarchyAuthority ?? null,
      isWritable: false,
    },
    pair: { value: input.pair ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authoritySigner),
      getAccountMeta(accounts.futarchyAuthority),
      getAccountMeta(accounts.pair),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetPairReduceOnlyInstructionDataEncoder().encode(
      args as SetPairReduceOnlyInstructionDataArgs,
    ),
    programAddress,
  } as SetPairReduceOnlyInstruction<
    TProgramAddress,
    TAccountAuthoritySigner,
    TAccountFutarchyAuthority,
    TAccountPair,
    TAccountSystemProgram
  >);
}

export type ParsedSetPairReduceOnlyInstruction<
  TProgram extends string = typeof OMNIPAIR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authoritySigner: TAccountMetas[0];
    futarchyAuthority: TAccountMetas[1];
    pair: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SetPairReduceOnlyInstructionData;
};

export function parseSetPairReduceOnlyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPairReduceOnlyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authoritySigner: getNextAccount(),
      futarchyAuthority: getNextAccount(),
      pair: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetPairReduceOnlyInstructionDataDecoder().decode(instruction.data),
  };
}
